{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
    <div class="text-center ">
        <h1 class="h4 text-gray-900 mb-4" >Mot de passe oublié!</h1>
    </div>
    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
        <div class="form-group text-gray-900">
            {#<label for="username">{{ 'resetting.request.username'|trans }}</label>#}
            <input type="text" id="username" name="username" required="required" placeholder="Nom d'utilisateur ou adresse e-mail" class="form-control form-control-user"/>
        </div>
        <div>
            <input type="submit" value="{{ 'resetting.request.submit'|trans }}"  class="btn btn-primary btn-user btn-block"/>
        </div>
<a href="{{ path('fos_user_security_login')}}" class="btn btn-default" style="color: #ffffff;">Retour</a>


    </form>

{% endblock %}